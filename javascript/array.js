// const circle={
//    radius:0.5,
//    area: function(x){
//        return 3.147*x**2;
//    }
// }
// console.log(circle.area(2))   
// //in and hasOwnpropertyx
// const person = { name: "Alice", age: 25 };
// console.log('name' in person); // true
// console.log('tostring' in person); // true (inherited from Object.prototype)
// console.log('gender' in person); // false
// The in operator checks whether a property exists anywhere on the object, including the prototype chain.
// The hasOwnProperty method checks whether the object has the specified property as its own property, not inherited through the prototype chain.
//call and apply are similar but when we want to pass parameter then we use apply parameter is passes in array like object.function.apply(2nd function,[parameter]) 
//but in call we pass parameter seperately without an array object.function.apply(2nd function,parameter)

p